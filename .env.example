# Open Library ETL Pipeline Configuration
# Copy this file to .env and adjust values as needed

# Google Books API Key (for cover image fallback)
# Get your key from: https://console.cloud.google.com/apis/credentials
GOOGLE_BOOKS_API_KEY=your_google_books_api_key_here

# Output directory for processed CSV files
OUTPUT_DIR=./output

# Input directory containing Open Library dump files
INPUT_DIR=.

# Processing limits (0 = process all records)
RECORD_LIMIT=0

# Quality thresholds
COMPLETENESS_THRESHOLD=40
PAGE_MIN=20
PAGE_MAX=1500

# Logging
LOG_LEVEL=INFO
